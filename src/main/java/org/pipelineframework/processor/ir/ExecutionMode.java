package org.pipelineframework.processor.ir;

/**
 * Enum representing execution mode configuration
 */
public enum ExecutionMode {
    /** Default execution mode */
    DEFAULT,
    /** Execution on virtual threads */
    VIRTUAL_THREADS
}